# Tkinter-languagetranslator-app
## setting up the project
### go to gui-googletranslator folder
#### use -> cd gui-googletranslator
### install all the required libraries for the project
#### use -> pip install -r requirements.txt
### run the app
